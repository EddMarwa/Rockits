// ParticleToggle removed — export a harmless stub to avoid import errors.
"use client";
export default function ParticleToggle(): null {

  // Intentionally return null. 
  // The toggle UI was removed; particles
  // are now controlled via URL params and prefers-reduced-motion.
  return null;
}

